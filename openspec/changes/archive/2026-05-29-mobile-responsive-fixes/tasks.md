## 1. Fix Mountain Background Pixelation

- [x] 1.1 Add mobile media query to `.alpine-bg` in `src/index.css` to set `background-attachment: scroll` below 768px

## 2. Fix Hero Section Mobile Overflow

- [x] 2.1 Reposition or hide the floating "Building things that matter" card on mobile so it doesn't overflow
- [x] 2.2 Constrain the hero photo container size on mobile screens

## 3. Fix Timeline Progress Bar on Mobile

- [x] 3.1 Fix the timeline progress bar left positioning to align with mobile card layout
- [x] 3.2 Add a visible left-side timeline line for mobile cards in ProfessionalPath

## 4. Fix Tech Stack Node Visualization on Mobile

- [x] 4.1 Replace absolute-positioned nodes with a flow layout (flex wrap) on mobile screens
- [x] 4.2 Scale down node padding and font sizes for mobile
